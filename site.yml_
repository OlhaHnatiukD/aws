- name: Installing aws machine 
  vars:
# vars_files:
#  - /etc/ansible/roles/common/aws_keysv2.yml
  roles:
    - common
  tasks:
   - name: make one instance

- name: Install and configure nginx and ansible
  hosts: webservers
  remote_user: root

  roles:
    - jenk 
